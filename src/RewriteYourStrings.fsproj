<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyName>RewriteYourStrings</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="RewriteYourStrings\RewriteMonad.fs" />
  </ItemGroup>

</Project>